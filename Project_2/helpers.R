# numeric variables
num_vars <- c(
  "App Usage Time (min/day)" = "app_usage_time",
  "Screen On Time (hours/day)" = "screen_on_time",
  "Battery Drain (mAh/day)" = "battery_drain",
  "Number of Apps Installed" = "num_app_installed",
  "Data Usage (MB/day)" = "data_usage",
  "Age" = "age"
)

# categorical variables
cat_vars <- c(
  "User ID" = "user_id",
  "Device Model" = "device_model",
  "Operating System" = "operating_system",
  "Gender" = "gender",
  "User Behavior Class" = "user_behavior_class"
)